<section class="hero-section">
    <img src="{{ asset('images/hero.png') }}" alt="Zaki Apparel" class="hero-bg-img">
    <div class="hero-overlay"></div>
    <div class="hero-content">
        <div class="hero-inner">
            <div class="hero-text">
                <h1>{{ __('landing.hero_title_line1') }}<span class="gold">{{ __('landing.hero_title_line2') }}</span></h1>
                <p>{{ __('landing.hero_subtitle') }}</p>
                <a href="#best-seller" class="hero-btn">{{ __('landing.hero_cta') }}</a>
            </div>
        </div>
    </div>
</section>